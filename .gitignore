# This file tells Git which files and folders to ignore.

# Ignore the folder containing all the downloaded packages.
# This folder can be very large and should be generated by running `npm install`.
node_modules

# Ignore the data files. These will be created and managed on the Render server's
# persistent disk and should not be part of your code repository.
gameData.json
appState.json

# Ignore npm debug logs, if any are created.
npm-debug.log*
